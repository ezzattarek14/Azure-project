server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' https: data: 'unsafe-inline' 'unsafe-eval';" always;

    # Enable Gzip Compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy Health Checks to Backend
    location /health {
        proxy_pass https://ca-backend-dev.ambitiousstone-dcd0ea41.centralus.azurecontainerapps.io/health;
        proxy_ssl_server_name on;
        proxy_set_header Host ca-backend-dev.ambitiousstone-dcd0ea41.centralus.azurecontainerapps.io;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout 5s;
        proxy_read_timeout 10s;
    }

    # Proxy API requests to Tier 2 Backend API container
    location /api/v1/ {
        proxy_pass https://ca-backend-dev.ambitiousstone-dcd0ea41.centralus.azurecontainerapps.io/api/v1/;
        proxy_ssl_server_name on;
        proxy_set_header Host ca-backend-dev.ambitiousstone-dcd0ea41.centralus.azurecontainerapps.io;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
